import React, { Component } from 'react';

class App extends Component {
  render() {
    return (
      <div>
        <button className="btn btn-primary">Rubix Manager</button>
      </div>
    );
  }
}

export default App;
